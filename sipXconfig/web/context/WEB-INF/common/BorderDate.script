<?xml version="1.0"?>
<!DOCTYPE script PUBLIC
	"-//Apache Software Foundation//Tapestry Script Specification 3.0//EN"
	"http://jakarta.apache.org/tapestry/dtd/Script_3_0.dtd">
<script>

<input-symbol key="date" class="java.lang.String" required="yes"/>

<body>
<![CDATA[
var currentDate = new Date("${date}");
var interval = 10000;

function updateDate() {
    var targetNode = dojo.byId("dateComponent");
    if (!targetNode) return;

    targetNode.innerHTML = dojo.date.format(currentDate, {
        datePattern: "EEE, dd MMM yyyy",
        timePattern: "h:mm aa"
    });

    currentDate.setMilliseconds(currentDate.getMilliseconds() + interval);
    setTimeout(updateDate, interval);
}

// Run when DOM is ready
dojo.addOnLoad(updateDate);
]]>
</body>
<initialization>
	updateDate();
</initialization>
</script>